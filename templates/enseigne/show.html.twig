{% extends 'base.html.twig' %}

{% block title %}Enseigne{% endblock %}

{% block body %}
    <h1>Enseigne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseigne.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enseigne_index') }}">back to list</a>

    <a href="{{ path('enseigne_edit', {'id': enseigne.id}) }}">edit</a>

    {{ include('enseigne/_delete_form.html.twig') }}
{% endblock %}
