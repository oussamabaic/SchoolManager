<form method="post" action="{{ path('cycle_delete', {'id': cycle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cycle.id) }}">
    <button data-toggle="tooltip" title="" class="pd-setting-ed" data-original-title="Trash">
        <a class="fa fa-trash-o" aria-hidden="true" href=""></a>
    </button>

</form>
