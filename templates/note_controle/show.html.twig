{% extends 'base.html.twig' %}

{% block title %}NoteControle{% endblock %}

{% block body %}
    <h1>NoteControle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note_controle.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ note_controle.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_controle_index') }}">back to list</a>

    <a href="{{ path('note_controle_edit', {'id': note_controle.id}) }}">edit</a>

    {{ include('note_controle/_delete_form.html.twig') }}
{% endblock %}
